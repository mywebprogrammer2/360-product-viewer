import axios from "../lib/axios"

const submitData = (payload) => {
    // try {
    //     const resp = axios.post('/submit/test',payload)
    //     return resp.data
        
    // } catch (error) {
    //     throw error
    // }
}

export {submitData}